// This file (index.tsx) is a TypeScript source file.
// For deployment to GitHub Pages with the current setup, 
// only the pre-transpiled index.js should be used and deployed.
// Please REMOVE this index.tsx file from the branch GitHub Pages deploys.

/*
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.tsx'; // In a TS environment, this would point to App.tsx

const rootElement = document.getElementById('root');
if (!rootElement) {
  throw new Error("Could not find root element to mount to");
}

const root = ReactDOM.createRoot(rootElement);
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
*/
